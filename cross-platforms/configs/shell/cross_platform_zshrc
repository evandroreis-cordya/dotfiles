# Cross-platform shell configuration file
# This file contains configurations that are common across all platforms

# Load cross-platform configurations
source ~/dotfiles/cross-platforms/configs/shell/cross_platform_exports
source ~/dotfiles/cross-platforms/configs/shell/cross_platform_functions
source ~/dotfiles/cross-platforms/configs/shell/cross_platform_options
source ~/dotfiles/cross-platforms/configs/shell/cross_platform_aliases

# Load platform-specific configurations
case "$(uname -s)" in
    Darwin*)
        source ~/dotfiles/macos/configs/shell/macos_specific
        ;;
    Linux*)
        source ~/dotfiles/linux/configs/shell/linux_specific
        ;;
    CYGWIN*|MINGW*|MSYS*)
        source ~/dotfiles/windows/configs/shell/windows_specific
        ;;
esac

# Load additional configurations if they exist
if [[ -d ~/dotfiles/cross-platforms/configs/shell/aliases ]]; then
    for file in ~/dotfiles/cross-platforms/configs/shell/aliases/*; do
        if [[ -f "$file" ]]; then
            source "$file"
        fi
    done
fi
